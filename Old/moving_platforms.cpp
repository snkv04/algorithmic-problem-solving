#include <bits/stdc++.h>
using namespace std;

using ll = long long;
int MOD = (int) 1e9 + 7; // 998244353;
int di[] = {0, 0, 1, -1}, dj[] = {1, -1, 0, 0};

void solve() {
    /**
     * l1 + t * s1 = l2 + t * s2 + kd
     * l1 + t*s1 - l2 - t*s2 = kd
     * (l1 - l2) + t * ()
     * 
     * 
     * t * (s1 - s2) - kd = (l2 - l1)
     */
    int n, m, H;
    cin >> n >> m >> H;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    int t = 1;
    cin >> t;
    while (t--) {
        solve();
    }

    return 0;
}
